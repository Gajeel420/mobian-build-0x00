# ZRAM Swap Configuration for PinePhone A64 (2GB RAM)
# Deploy to: /etc/default/zramswap

# Compression algorithm - lz4 is fast and efficient on ARM
ALGO=lz4

# Percentage of RAM to use for ZRAM (50% of 2GB = ~1GB)
PERCENT=50

# Swap priority (higher than disk-based swap)
PRIORITY=100
